# RFC-0.27-001 S4 (`doc-links`): links found broken and deliberately left
# that way, because fixing them needs a decision this line does not make —
# "fix what is mechanical, record what is not" (implementation handoff §3).
#
# Format: one entry per line, `<file> -> <link as written>`. Blank lines and
# `#` comments are ignored. Any broken relative link not listed here fails
# the check.

# No entries at present. The last two — references to ADR-0003 and ADR-0004
# from before RFC 045's renumbering — were repointed on 2026-09-15: each
# superseded ADR's own Status line named its successor, so they were never the
# investigative work this file had recorded them as. Since the same day an
# entry that no longer matches a broken link fails the check, so this list
# cannot quietly outlive the problem it records.
