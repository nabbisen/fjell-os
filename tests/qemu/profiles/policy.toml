# RFC 040 (v0.2): cap-broker policy enforcement.
# Requires IPC routing to cap-broker; markers added in v0.2.2.

name             = "policy"
timeout_secs     = 60
expected_markers = []
extra_args       = []
