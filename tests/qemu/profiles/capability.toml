# RFC 031 (v0.2): require_cap kind check — wrong kind rejected.
# NEG_TEST service emits this when sys_mmio_map with an Endpoint cap fails.

name             = "capability"
timeout_secs     = 90
expected_markers = [
  "NEG:CAP:WRONG_KIND_REJECTED:PASS",
]
extra_args       = []
