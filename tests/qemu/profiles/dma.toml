# RFC 036 (v0.2): DMA rights check + explicit revoke.
# NEG_TEST service emits both markers.

name             = "dma"
timeout_secs     = 60
expected_markers = [
  "NEG:DMA:RIGHTS_CHECK:PASS",
  "NEG:DMA:REVOKE_EXPLICIT:PASS",
]
extra_args       = []
