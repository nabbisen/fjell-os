# RFC 036 (v0.2): DmaRegion state machine + zeroize-on-exit.

name             = "dma"
timeout_secs     = 60
expected_markers = [
  "NEG:DMA:ZEROIZE_ON_EXIT:PASS",
  "NEG:DMA:REVOKE_EXPLICIT:PASS",
  "NEG:DMA:RIGHTS_CHECK:PASS",
]
extra_args       = []
