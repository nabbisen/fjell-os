# Syscall Reference

See `cargo doc --no-deps -p fjell-syscall` for the generated API reference.

Core syscalls: `sys_ipc_call`, `sys_ipc_recv`, `sys_cap_copy`, `sys_cap_drop`, `sys_yield`.

*References RFC 028 (syscall inventory).*
