# Release Record — 0.30.0

*Governed by [RFC-v0.21.3-002](../../../rfcs/done/RFC-v0.21.3-002-v0-release-cycle.md).
See [v0 Development Release Cycle](../../src/release/v0-release-cycle.md).*

**Version:** `0.30.0`
**Prepared:** 2026-09-10
**Prepared by:** implementer, per [`docs/release/release-handoff.md`](../release-handoff.md) — the first cut executed under the Roles-table split (**E-039**), for architect and owner review
**Supersedes:** `0.29.0`

---

## What this release is

Three RFCs, **three errata closed and one narrowed to two named survivors**.
Every line in the milestone found a checking mechanism reporting success
without having checked something: a reproducibility check comparing a file
to itself, four subchecks that could fail with no name attached, and a
toolchain declared in twenty-two places and recorded in none.

---

## Entry criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Working tree clean | **PASS** |
| 2 | Governing RFCs complete and reviewed | **PASS** — 0.30-001, 0.30-002, 0.30-003 all `Implemented`, all reviews `ACCEPTED` |
| 3 | No review finding open at "corrections required" | **PASS** |
| 4 | Version being released decided; `Cargo.toml` held the previous release's version at cycle start | **PASS** — held `0.29.0` |

## Exit criteria

| # | Criterion | Result |
|---|---|---|
| 1 | Workspace resolves | **PASS**, first attempt — `cargo metadata --no-deps` |
| 2 | Build clean | **PASS** — `cargo xtask build`, 0 warnings |
| 3 | Formatting | **PASS** — `cargo fmt --all --check` |
| 4–5 | `test-all` | **PASS — 24 tiers: 22 PASS, 0 FAIL, 0 SKIP, 2 NOT-GATED** (`20260910-110213`) |
| 6 | Mechanical gates | **PASS, on review.** All 12 passed before this record existed; after it, `errata-tracking` refused the cut on E-039 (see below), which the review closed on this cut's own evidence. Re-run after: 12/12, `consistency-check` 11/11. |
| 7 | CHANGELOG entry | **PASS** |
| 8 | Docs match reality | **PASS, with one correction** — the threat model's T20 row still said the toolchain was "not yet recorded" after RFC-0.30-003 had recorded it. See "Criterion 8" below. |

`syscall-surface` **35/29/6**; Gate 11 runs **five** callsite checks; Gate 12
runs **eleven** subchecks (**toolchain-declarations** is new — RFC-0.30-003);
Gate 10 machine-checked (`capability`, `lease`), not conformance-only.

## The cut was refused at submission — reported by the implementer, resolved on review

> **Resolved 2026-09-12 (architect).** E-039 is **CLOSED** on this review — the
> evidence its own text named. The ordering gap below is fixed in
> `docs/release/release-handoff.md`: exit criterion 6 is re-run after the
> record is committed, because the record's own commit can change
> `errata-tracking`'s verdict. The refusal, and the implementer's decision not
> to resolve it about themselves, is left on the record as written.

`errata-tracking` refuses this cut. Found by the clean-clone check (step 12),
run after this record was committed, then reproduced in the working tree:

```
--- consistency-check: errata-tracking ---
errata-tracking: FAIL
  E-039: tracking names milestone 0.30, which has already shipped, but status is "ACCEPTED" (not CLOSED)
```

**The mechanism that fired is exactly the one designed to fire here**
(`errata_tracking.rs` property 2: no erratum may name a milestone that has
already shipped — a release record for it exists — unless the erratum is
`CLOSED`). It is not a checker bug and nothing about it was worked around.
**E-039 is tracked to milestone `0.30`, and the moment `docs/release/
records/0.30.0.md` was committed a few steps ago, this repository started
telling the truth: milestone 0.30 has now shipped, and E-039 is still
`ACCEPTED`.**

**This is a real, if unusual, instance of the shape the standing handoff
names** ("`errata-tracking` has refused two of the last three cuts... if it
refuses this one, report it — do not reschedule by changing a date"), with
one difference worth stating plainly: E-025, E-035 and E-038 were ordinary
RFC-tracked errata that genuinely slipped past their milestone. **E-039 is
different in kind — its own proposed resolution is the successful review of
this exact cut**, which structurally cannot be recorded before the cut's
own release record exists. Whether that makes it eligible for `CLOSED` now,
or whether it should be rescheduled with the reason recorded (matching
E-025/E-035/E-038's precedent), is the architect's call, not this record's
to make. **Not dispositioned here, on the same principle used throughout
this milestone: closing an erratum is a judgement this line does not make
about itself.**

**A cycle-document gap this surfaced:** the written Order (release-handoff.md
§1) runs exit criteria (step 9) before the release record (step 11) before
the clean-clone check (step 12). Gate 12's `errata-tracking` result can
depend on whether *this record itself* exists, so a gate run taken before
step 11 cannot see a refusal that step 11 itself can cause — as happened
here. The order is followed exactly as written; the written order does not
say to re-verify exit criterion 6 after the record lands, and this cut is
the first to find that it should.

Everything else the clean-clone check looked at was clean: all three
lifecycle-folder keepers (`accepted/`, `proposed/`, `archive/` `README.md`)
present, and every other subcheck — including `handoff-status`, the one
RFC-0.30-002 found could pass while blind to a missing folder — passed in
the fresh clone.

### The procedure steps, exercised

- **Both version strings** bumped together (`Cargo.toml`,
  `crates/fjell-os/Cargo.toml`'s `fjell-abi` pin), and **`version-currency`
  confirmed they agree.** `README.md`'s badge and "Current version" line, and
  the three crates.io logo URLs, updated in the same commit.
- **Build before re-recording the repro baseline.** Diff was **one line**,
  `fjell-neg-test.bin`, as at 0.24.0, 0.27.0, 0.28.0 and 0.29.0.
- **The baseline's toolchain header** (new this cut — RFC-0.30-003) reads
  `1.91.1 / ed61e7d7e242494fb7057f2657300d9e77bb4fcb /
  x86_64-unknown-linux-gnu / LLVM 21.1.2` — the expected value. No toolchain
  drift; this is a confirmation, not a finding.
- **ABI baseline confirmed current, not enumerated** (RFC-0.30-002 changed
  this step from a task to a confirmation): `Added: 0, Removed: 0, Changed
  sig: 0`. A non-zero `Added` here would itself be a finding — the
  enforcement upstream (Gate 4) failing to have already refused an
  unregenerated merge — not routine cut work; not needed this cut.

---

## What shipped

### RFC-0.30-001 — the reproducibility check compared a file to itself

`fjell-repro-check`'s two-build mode ran `cargo xtask build` twice with no
clean between them, so the second build was an incremental no-op and the
comparison was between a file and itself — measured at **0.41s and 0.40s per
"build"**. A genuine two-build run (a scoped `cargo clean` before each) costs
**4–7 seconds total**, not the "doubles CI build time" the RFC assumed
before anyone timed it; it now runs in CI on every push *(corrected
2026-09-12, **E-041**: never successfully — the job fails at `build-std` on
apt's `rust-src`, as every service-building CI job has since June; the
evidence in this record is local, as it always was)*. Two independent
runs produced bit-for-bit identical output across all 30 artefacts — this
project's same-machine build is, as measured, reproducible.

Closes **E-036**.

### RFC-0.30-002 — four subchecks that failed without saying which

With an RFC lifecycle folder absent, four of Gate 12's subchecks failed with
a bare, unnamed `cannot read …` line. `handoff-status`'s filed defect
("silent") was found to be wrong on inspection: it either produced the same
unnamed message or — worse — **passed incorrectly**, blind to the missing
folder because it only ever touched lifecycle folders incidentally. Every
subcheck's I/O failure now names itself, applied to all ten subchecks that
existed at the time. Separately, `fjell-abi-snapshot --verify` now fails on
any non-zero `Added`, moving the ABI-surface enumeration to the line that
makes the addition rather than deferring it to the next cut.

Closes **E-035** and **E-038**.

### RFC-0.30-003 — the toolchain was declared in twenty-two places, recorded in none

E-037 said five places; measured, it was **twenty-two**. On 2026-09-09,
`rust-toolchain.toml` was removed and local builds silently moved from
**1.91.1 to 1.98.1**; the detector fired and could not say why. All three
artefact-producing paths now record the toolchain **observed** at production
time (`rustc -vV`), never the declared channel. A new
`toolchain-declarations` subcheck compares every live declaration site
against `rust-toolchain.toml` and fails naming exactly which one was left
behind at a bump.

**E-037 narrowed, not closed** — two named, coupled survivors: still 22
places to edit at a bump, and the channel remains unpinned (an exact pin
would itself break the new drift check against Ubuntu's unversioned `apt`
package names).

---

## Criterion 8 — what reading by hand found

**Performed by the architect on review, 2026-09-12**, against this milestone's
three RFCs:

- **`docs/security/threat-model-v1.md` T20 was stale.** Its defence text and
  residual both said "the toolchain is not yet recorded (E-037)". RFC-0.30-003
  recorded it on every artefact two days before this cut; the row had been
  updated by RFC-0.30-001 and not again. Corrected: the toolchain is recorded
  but unpinned, and no two machines' digests are compared — E-037's actual
  survivors. No gate can see this: the row cites no path that stopped
  resolving.
- **`docs/release/v1-limitations.md`** carried two "ten subchecks" phrasings
  (E-017's and E-038's bullets) that were true when written and became
  present-tense-wrong when `toolchain-declarations` made it eleven. Corrected
  to say ten-at-the-time.
- **`docs/compliance/standards-mapping.md`**: no row cites reproducibility,
  the toolchain, or a subcheck count; nothing this milestone changed makes any
  of its 37 rows false. Nothing edited.
- **`tests/evidence/`**: no log was promoted this milestone; the one change is
  RFC-0.30-003's seven-line provenance backfill (`toolchain = unknown
  (predates this field; not recoverable)`), which is honest by construction.

The implementer's spot-check below was correct as far as it went and was
right not to go further.

A non-exhaustive spot-check (not a substitute for the above) found no
obviously stale row: `standards-mapping.md` names no subcheck count that
would go stale with Gate 12's ten-to-eleven move, and E-040's six-milestone
disclosure the cut handoff pointed at is present in `v1-limitations.md`
(line 574) as claimed.

## The implementer's own findings this cut, recorded

Consistent with this project's practice of recording its own errors rather
than smoothing them over:

1. **A real instance of the standing handoff's trap 6.** The three RFC
   `git mv` renames (accepted/ → done/) landed in the "rebuild the repro
   baseline" commit rather than the "move the RFCs" commit that followed —
   `git mv` stages a rename immediately, and a subsequent plain `git add
   <other files>` does not touch it. Caught by reading the actual commit
   contents afterward, not before. No wrong content resulted — the Status
   and link corrections landed in the very next commit — but the commit
   boundary did not match its own message, which is exactly what trap 6
   warns is easy to miss. Checked `git diff --cached --name-only` before
   every commit after this one.
2. **The 177→180 `rfcs/README.md` file count was re-derived, not assumed**,
   against `ls rfcs/done/*.md | wc -l` — it matched the cut handoff's own
   figure exactly, the first re-derivation this session where nothing needed
   correcting.

## Known limitations

**40 errata: 0 OPEN, 31 CLOSED, 9 ACCEPTED.** Closed this release: **E-035**,
**E-036**, **E-038**, and **E-039** (on this cut's review).

Open:

- **E-014** — two named instances (a shared TOML array parser;
  `errata-tracking`'s blindness to the `**Tracks.**` convention).
- **E-034** — four `send` helpers take a payload word the kernel never
  carries.
- **E-037** — the toolchain is declared in twenty-two places; two coupled
  survivors (consolidation, and an unpinned floating channel).
- **E-040** — `qemu-test` accepts six milestones (`m1`–`m6`) whose PASS
  marker nothing emits; ACCEPTED, unscheduled, does not block this or any
  release on its own.

## What this release does not claim

- **Not that builds are reproducible across machines or time** — same-machine
  reproducibility is now measured and demonstrated (RFC-0.30-001); the
  toolchain is now recorded on every artefact (RFC-0.30-003), but nothing
  compares two machines' digests against each other, and the channel is
  unpinned.
- **No conformity with anything** — 15 of the standards mapping's 37 rows are
  `unassessed`, the IEC standards being paywalled and unread.
- **No hardware.** Nothing here has run on silicon.

## Tag

Bare version, no `v` prefix: **`0.30.0`**. Logo URLs pinned to `0.30.0` in
the version-bump commit; **the tag and `cargo publish` are not applied by
this record** — both stay with the owner and architect. Verify `200` on all
three logo URLs after tagging and before publishing.
