//! Auto-generated typed emitters for catalog v1.
//! Generated by `cargo xtask toolkit regenerate` (RFC-v0.14-003).
//! DO NOT EDIT — regenerate with `cargo xtask toolkit regenerate`.

use fjell_semantic_v1::{encode, FieldValue, SemanticError};

/// Arguments for `UPDATE.STAGING_STARTED` (tag 0x0100).
pub struct UpdateStagingStartedArgs {
    pub candidate_id: u32,
    pub channel_id: u32,
    pub counter: u32,
}

/// Emit `UPDATE.STAGING_STARTED` (tag 0x0100).
pub fn emit_update_staging_started(
    args: &UpdateStagingStartedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.candidate_id),
        FieldValue::U32(args.channel_id),
        FieldValue::U32(args.counter),
    ];
    encode(0x0100, tick, fields, out)
}

/// Arguments for `UPDATE.STAGING_ADVANCED` (tag 0x0101).
pub struct UpdateStagingAdvancedArgs {
    pub candidate_id: u32,
    pub from_state: u16,
    pub to_state: u16,
}

/// Emit `UPDATE.STAGING_ADVANCED` (tag 0x0101).
pub fn emit_update_staging_advanced(
    args: &UpdateStagingAdvancedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.candidate_id),
        FieldValue::U16(args.from_state),
        FieldValue::U16(args.to_state),
    ];
    encode(0x0101, tick, fields, out)
}

/// Arguments for `UPDATE.STAGING_FAILED` (tag 0x0102).
pub struct UpdateStagingFailedArgs {
    pub candidate_id: u32,
    pub error_code: u16,
}

/// Emit `UPDATE.STAGING_FAILED` (tag 0x0102).
pub fn emit_update_staging_failed(
    args: &UpdateStagingFailedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.candidate_id),
        FieldValue::U16(args.error_code),
    ];
    encode(0x0102, tick, fields, out)
}

/// Arguments for `UPDATE.STAGING_CONFIRMED` (tag 0x0103).
pub struct UpdateStagingConfirmedArgs {
    pub candidate_id: u32,
    pub counter: u32,
    pub slot: u8,
}

/// Emit `UPDATE.STAGING_CONFIRMED` (tag 0x0103).
pub fn emit_update_staging_confirmed(
    args: &UpdateStagingConfirmedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.candidate_id),
        FieldValue::U32(args.counter),
        FieldValue::U8(args.slot),
    ];
    encode(0x0103, tick, fields, out)
}

/// Arguments for `UPDATE.ROLLBACK_BLOCKED` (tag 0x0110).
pub struct UpdateRollbackBlockedArgs {
    pub channel_id: u32,
    pub attempted: u32,
    pub min_counter: u32,
}

/// Emit `UPDATE.ROLLBACK_BLOCKED` (tag 0x0110).
pub fn emit_update_rollback_blocked(
    args: &UpdateRollbackBlockedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.channel_id),
        FieldValue::U32(args.attempted),
        FieldValue::U32(args.min_counter),
    ];
    encode(0x0110, tick, fields, out)
}

/// Arguments for `UPDATE.ROLLBACK_TO_PREVIOUS_SLOT` (tag 0x0111).
pub struct UpdateRollbackToPreviousSlotArgs {
    pub previous_slot: u8,
    pub reason_code: u16,
}

/// Emit `UPDATE.ROLLBACK_TO_PREVIOUS_SLOT` (tag 0x0111).
pub fn emit_update_rollback_to_previous_slot(
    args: &UpdateRollbackToPreviousSlotArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U8(args.previous_slot),
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0111, tick, fields, out)
}

/// Arguments for `ATTEST.RECORD_SIGNED` (tag 0x0120).
pub struct AttestRecordSignedArgs {
    pub record_id: u32,
    pub profile: u8,
    pub provider_id: u32,
}

/// Emit `ATTEST.RECORD_SIGNED` (tag 0x0120).
pub fn emit_attest_record_signed(
    args: &AttestRecordSignedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.record_id),
        FieldValue::U8(args.profile),
        FieldValue::U32(args.provider_id),
    ];
    encode(0x0120, tick, fields, out)
}

/// Arguments for `ATTEST.RECORD_VERIFY_FAILED` (tag 0x0121).
pub struct AttestRecordVerifyFailedArgs {
    pub record_id: u32,
    pub error_code: u16,
}

/// Emit `ATTEST.RECORD_VERIFY_FAILED` (tag 0x0121).
pub fn emit_attest_record_verify_failed(
    args: &AttestRecordVerifyFailedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.record_id),
        FieldValue::U16(args.error_code),
    ];
    encode(0x0121, tick, fields, out)
}

/// Arguments for `SECURITY.REGISTRY_ENFORCING` (tag 0x0130).
pub struct SecurityRegistryEnforcingArgs {
    pub providers_registered: u8,
}

/// Emit `SECURITY.REGISTRY_ENFORCING` (tag 0x0130).
pub fn emit_security_registry_enforcing(
    args: &SecurityRegistryEnforcingArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U8(args.providers_registered),
    ];
    encode(0x0130, tick, fields, out)
}

/// Arguments for `SECURITY.PROVIDER_FAULTED` (tag 0x0131).
pub struct SecurityProviderFaultedArgs {
    pub provider_id: u32,
    pub reason_code: u16,
}

/// Emit `SECURITY.PROVIDER_FAULTED` (tag 0x0131).
pub fn emit_security_provider_faulted(
    args: &SecurityProviderFaultedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.provider_id),
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0131, tick, fields, out)
}

/// Arguments for `SECURITY.KEYRING_EPOCH_ADVANCED` (tag 0x0132).
pub struct SecurityKeyringEpochAdvancedArgs {
    pub purpose: u8,
    pub old_epoch: u32,
    pub new_epoch: u32,
}

/// Emit `SECURITY.KEYRING_EPOCH_ADVANCED` (tag 0x0132).
pub fn emit_security_keyring_epoch_advanced(
    args: &SecurityKeyringEpochAdvancedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U8(args.purpose),
        FieldValue::U32(args.old_epoch),
        FieldValue::U32(args.new_epoch),
    ];
    encode(0x0132, tick, fields, out)
}

/// Arguments for `NET.LINK_UP` (tag 0x0140).
pub struct NetLinkUpArgs {
    pub device_id: u32,
    pub mtu: u16,
}

/// Emit `NET.LINK_UP` (tag 0x0140).
pub fn emit_net_link_up(
    args: &NetLinkUpArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.device_id),
        FieldValue::U16(args.mtu),
    ];
    encode(0x0140, tick, fields, out)
}

/// Arguments for `NET.LINK_DOWN` (tag 0x0141).
pub struct NetLinkDownArgs {
    pub device_id: u32,
    pub reason_code: u16,
}

/// Emit `NET.LINK_DOWN` (tag 0x0141).
pub fn emit_net_link_down(
    args: &NetLinkDownArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.device_id),
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0141, tick, fields, out)
}

/// Arguments for `NET.SXT_CHANNEL_OPENED` (tag 0x0142).
pub struct NetSxtChannelOpenedArgs {
    pub channel_id: u32,
    pub kind: u8,
    pub server_name: [u8; 16],
}

/// Emit `NET.SXT_CHANNEL_OPENED` (tag 0x0142).
pub fn emit_net_sxt_channel_opened(
    args: &NetSxtChannelOpenedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.channel_id),
        FieldValue::U8(args.kind),
        FieldValue::Bytes16(args.server_name),
    ];
    encode(0x0142, tick, fields, out)
}

/// Arguments for `NET.SXT_CHANNEL_CLOSED` (tag 0x0143).
pub struct NetSxtChannelClosedArgs {
    pub channel_id: u32,
    pub reason_code: u16,
}

/// Emit `NET.SXT_CHANNEL_CLOSED` (tag 0x0143).
pub fn emit_net_sxt_channel_closed(
    args: &NetSxtChannelClosedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.channel_id),
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0143, tick, fields, out)
}

/// Arguments for `RECOVERY.ENTERED` (tag 0x0150).
pub struct RecoveryEnteredArgs {
    pub reason_code: u16,
}

/// Emit `RECOVERY.ENTERED` (tag 0x0150).
pub fn emit_recovery_entered(
    args: &RecoveryEnteredArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0150, tick, fields, out)
}

/// Arguments for `RECOVERY.EXITED` (tag 0x0151).
pub struct RecoveryExitedArgs {
    pub outcome_code: u16,
}

/// Emit `RECOVERY.EXITED` (tag 0x0151).
pub fn emit_recovery_exited(
    args: &RecoveryExitedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U16(args.outcome_code),
    ];
    encode(0x0151, tick, fields, out)
}

/// Arguments for `PLATFORM.PROFILES_READY` (tag 0x0160).
pub struct PlatformProfilesReadyArgs {
    pub platform_digest: [u8; 32],
    pub board_digest: [u8; 32],
}

/// Emit `PLATFORM.PROFILES_READY` (tag 0x0160).
pub fn emit_platform_profiles_ready(
    args: &PlatformProfilesReadyArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::Bytes32(args.platform_digest),
        FieldValue::Bytes32(args.board_digest),
    ];
    encode(0x0160, tick, fields, out)
}

/// Arguments for `HEALTH.TARGET_REACHED` (tag 0x0170).
pub struct HealthTargetReachedArgs {
    pub target_id: u32,
    pub status: u8,
}

/// Emit `HEALTH.TARGET_REACHED` (tag 0x0170).
pub fn emit_health_target_reached(
    args: &HealthTargetReachedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.target_id),
        FieldValue::U8(args.status),
    ];
    encode(0x0170, tick, fields, out)
}

/// Arguments for `HEALTH.TARGET_FAILED` (tag 0x0171).
pub struct HealthTargetFailedArgs {
    pub target_id: u32,
    pub reason_code: u16,
}

/// Emit `HEALTH.TARGET_FAILED` (tag 0x0171).
pub fn emit_health_target_failed(
    args: &HealthTargetFailedArgs,
    tick: u64,
    out: &mut [u8],
) -> Result<usize, SemanticError> {
    let fields: &[FieldValue] = &[
        FieldValue::U32(args.target_id),
        FieldValue::U16(args.reason_code),
    ];
    encode(0x0171, tick, fields, out)
}


#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn emit_update_staging_started_round_trip() {
        let args = UpdateStagingStartedArgs { candidate_id: 1u32, channel_id: 1u32, counter: 1u32 };
        let mut buf = [0u8; 128];
        let n = emit_update_staging_started(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_update_staging_advanced_round_trip() {
        let args = UpdateStagingAdvancedArgs { candidate_id: 1u32, from_state: 1u16, to_state: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_update_staging_advanced(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_update_staging_failed_round_trip() {
        let args = UpdateStagingFailedArgs { candidate_id: 1u32, error_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_update_staging_failed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_update_staging_confirmed_round_trip() {
        let args = UpdateStagingConfirmedArgs { candidate_id: 1u32, counter: 1u32, slot: 1u8 };
        let mut buf = [0u8; 128];
        let n = emit_update_staging_confirmed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_update_rollback_blocked_round_trip() {
        let args = UpdateRollbackBlockedArgs { channel_id: 1u32, attempted: 1u32, min_counter: 1u32 };
        let mut buf = [0u8; 128];
        let n = emit_update_rollback_blocked(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_update_rollback_to_previous_slot_round_trip() {
        let args = UpdateRollbackToPreviousSlotArgs { previous_slot: 1u8, reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_update_rollback_to_previous_slot(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_attest_record_signed_round_trip() {
        let args = AttestRecordSignedArgs { record_id: 1u32, profile: 1u8, provider_id: 1u32 };
        let mut buf = [0u8; 128];
        let n = emit_attest_record_signed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_attest_record_verify_failed_round_trip() {
        let args = AttestRecordVerifyFailedArgs { record_id: 1u32, error_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_attest_record_verify_failed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_security_registry_enforcing_round_trip() {
        let args = SecurityRegistryEnforcingArgs { providers_registered: 1u8 };
        let mut buf = [0u8; 128];
        let n = emit_security_registry_enforcing(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_security_provider_faulted_round_trip() {
        let args = SecurityProviderFaultedArgs { provider_id: 1u32, reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_security_provider_faulted(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_security_keyring_epoch_advanced_round_trip() {
        let args = SecurityKeyringEpochAdvancedArgs { purpose: 1u8, old_epoch: 1u32, new_epoch: 1u32 };
        let mut buf = [0u8; 128];
        let n = emit_security_keyring_epoch_advanced(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_net_link_up_round_trip() {
        let args = NetLinkUpArgs { device_id: 1u32, mtu: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_net_link_up(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_net_link_down_round_trip() {
        let args = NetLinkDownArgs { device_id: 1u32, reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_net_link_down(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_net_sxt_channel_opened_round_trip() {
        let args = NetSxtChannelOpenedArgs { channel_id: 1u32, kind: 1u8, server_name: [0u8; 16] };
        let mut buf = [0u8; 128];
        let n = emit_net_sxt_channel_opened(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_net_sxt_channel_closed_round_trip() {
        let args = NetSxtChannelClosedArgs { channel_id: 1u32, reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_net_sxt_channel_closed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_recovery_entered_round_trip() {
        let args = RecoveryEnteredArgs { reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_recovery_entered(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_recovery_exited_round_trip() {
        let args = RecoveryExitedArgs { outcome_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_recovery_exited(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_platform_profiles_ready_round_trip() {
        let args = PlatformProfilesReadyArgs { platform_digest: [0u8; 32], board_digest: [0u8; 32] };
        let mut buf = [0u8; 128];
        let n = emit_platform_profiles_ready(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_health_target_reached_round_trip() {
        let args = HealthTargetReachedArgs { target_id: 1u32, status: 1u8 };
        let mut buf = [0u8; 128];
        let n = emit_health_target_reached(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
    #[test]
    fn emit_health_target_failed_round_trip() {
        let args = HealthTargetFailedArgs { target_id: 1u32, reason_code: 1u16 };
        let mut buf = [0u8; 128];
        let n = emit_health_target_failed(&args, 1000, &mut buf).expect("emit ok");
        assert!(n > 0);
    }
}
