# generated by `cargo xtask schema dump` — do not edit; the encoder is the description
# crate: fjell-diag-format
# format: DiagnosticBundleV1
# version: v0.6.0 (frozen)
# note: 2026-09-24: this file used to claim `export_tick`, `source_service`, `entry_count` and `entries[]` of tag/tick/len/body; the encoder writes `created_tick`, `measurement_head`, `last_attestation` and two counted groups, `audit_events` and `semantic_intents`. Corrected toward the encoder; no byte moved (RFC-0.33-003 D3).
domain "FJELL-DIAG-V1"
field schema_version                 u16 LE
field bundle_id                      u8 [8]
field created_tick                   u64 LE
field measurement_head               u8 [32]
field last_attestation               u8 [32]
field audit_event_count              u8
group audit_events max 64
field audit_events[].seq             u32 LE
field audit_events[].kind_tag        u16 LE
field audit_events[].code            u16 LE
field audit_events[].at_tick         u64 LE
field semantic_intent_count          u8
group semantic_intents max 32
field semantic_intents[].seq         u32 LE
field semantic_intents[].intent_tag  u16 LE
field semantic_intents[].code        u16 LE
field semantic_intents[].at_tick     u64 LE
end
