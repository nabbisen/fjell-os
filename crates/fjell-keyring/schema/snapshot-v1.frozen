# generated by `cargo xtask schema dump` — do not edit; the encoder is the description
# crate: fjell-keyring
# format: KeyringSnapshotV1
# version: v0.6.0 (frozen)
# note: 2026-09-24: this file used to claim `purpose_count` and six `purpose[0..6]` records with a 64-byte `anchor_bytes`; the encoder writes the SNAP-V1 domain, `anchor_count` and 28 slots of present/purpose/algorithm/authority/epoch/reserved/key_len/key_bytes. Corrected toward the encoder; no byte moved (RFC-0.33-003 D3).
domain "FJELL-KEYRING-V1"
domain "SNAP-V1"
field schema_version     u16 LE
field anchor_count       u8
group slots max 28
field slots[].present    u8
field slots[].purpose    u8
field slots[].algorithm  u8
field slots[].authority  u8
field slots[].epoch      u32 LE
zeros slots[].reserved   u8 [1] = 0
field slots[].key_len    u8
field slots[].key_bytes  u8 [slots[].key_len]
end
